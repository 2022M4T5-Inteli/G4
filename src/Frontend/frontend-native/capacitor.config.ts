import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.keepgrowing',
  appName: 'keepgrowing-neo',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
